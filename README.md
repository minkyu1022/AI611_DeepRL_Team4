# Deep RL Final Assignment

This submission is split into two clearly named parts:

- `omatg-irl-reproduce/` - reproduction code and notebook for OMatG-IRL Section 4.2.
- `omatg-irl-extension/` - Colab-style extension report notebook for de novo generation with a S.U.N. reward.

Open `omatg-irl-extension/omatg-irl-extension.ipynb` first for the assignment report story.
It references `omatg-irl-reproduce/` as the supplementary implementation for the reproduced paper baseline.
